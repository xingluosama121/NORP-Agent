# 视觉 + 可操作 Agent（外挂模块）设计文档

> 版本：v0.1（设计稿，尚未实现）
> 状态：方案定稿，待评审进入实现
> 关联：本文档描述的是一套**独立于主架构之外**的「视觉 + 操作电脑」能力模块。

---

## 0. 一句话定位

让 Agent 能够**主动地「看」用户在屏幕上指定的任意窗口，并在用户显式授权 + 分级确认 + 可熔断的安全约束下，对窗口执行键鼠操作**。这是一个「有物理副作用」的能力，因此安全模型与现有 LLM 内容安全系统**完全不同，从零设计、独立部署、可被物理级熔断**。

---

## 1. 目标与非目标

### 1.1 目标（Goal）

- [ ] 用户可**主动选择**一个任意窗口（OBS 式窗口捕获，被遮挡/移出屏幕也能看到画面）。
- [ ] Agent 通过**工具调用（插件接入）**触发视觉理解与键鼠操作，不阻塞主线程。
- [ ] 视觉 provider 兼容 **OpenAI 兼容协议 / Anthropic / llama.cpp（本地）** 三类，可扩展。
- [ ] 坐标换算经 **反缩放 + DPI + ClientToScreen** 闭环，配合**负反馈（动作-验证-收敛）**自纠偏。
- [ ] 完整的**分级确认 + 三态熔断 + 全局热键物理熔断 + 前台在场检测 + override 让渡协议**。
- [ ] 主架构**记录每一次操作**（含被否决的操作），全程可审计、可回放。
- [ ] 提供**独立标定台**，用于脱离 Agent 链路单独调试 override / 熔断 / 坐标换算（全 dry-run）。

### 1.2 非目标（Non-Goal，用于防止范围蔓延）

- ❌ **不做提权**：绝对不使用 UAC、驱动、ring0、全局钩子。外挂以普通用户权限运行。
- ❌ **不做全屏捕获**：只捕获用户显式选择的单个窗口，不捕获整个桌面。
- ❌ **不做隐蔽后台运行**：Agent 操作电脑时用户必须在场；用户离开则自动暂停。
  - 运行期间屏幕**左上角常驻醒目横幅**：「⚠ Agent 正在操控电脑 —— 随时可介入，按 Ctrl+End 立即停止」。自动化**可见**，绝不无提示地在后台运行。
- ❌ **不集成进主架构信任域**：外挂是独立进程，主架构只发指令 + 授权令牌 + 读审计。
- ❌ **不复用现有 LLM 安全框架**：现有 `permission_cascade` / `norp_safe` / `jailbreak_guard` / `runtime_check` 是内容/代码级安全，与本模块的「物理副作用级」安全是两套东西，**逻辑、状态机、裁决器、审计格式全部从零定义**。
- ❌ **第一版不做视频**：视频整段 base64 上传当前不可用，抽帧依赖 ffmpeg/opencv，暂缓。先做图片链路。

---

## 2. 宪法级约束（不可被任何用户消息 / prompt 注入覆盖）

这些约束是硬性的，优先级高于任何运行时指令：

1. **不提权**：外挂进程启动时自检 `IsUserAnAdmin`（token 检查），检测到管理员权限则**拒绝启动**。
2. **被动性**：外挂没有「自主发起」能力，只能响应主架构发来的、带有效令牌的指令。它唯一的主动行为是「上报状态」和「自检」。
3. **用户第一权威**：任何 L1+ 操作必须经用户闸门；override 是用户**主动让渡**，不是系统自动判定。
4. **失效安全 > 失效可控 > 失效危险**：宁可因熔断而「什么都不做」，也不允许「失控地继续做」。
5. **安全凌驾 Agent**：裁决权在外挂进程内，Agent 只能「申请」不能「裁决自己」。
6. **不阻塞主线程**：所有视觉调用、捕获、操作均为异步，禁止任何同步阻塞主事件循环。

---

## 3. 进程拓扑（外挂独立进程 + 插件桥接）

```
┌────────────────────────────────────────────────────────────┐
│  主架构（NORP Agent 核心）                                    │
│  - 通过 plugin_system 注册「视觉/操作」工具（TOOLS）            │
│  - 只负责：发指令、发一次性授权令牌、分级、审计日志              │
│  - 不直接碰窗口 / 键鼠 / 驱动 / 捕获                          │
└──────────────┬─────────────────────────────────────────────┘
               │ 本地 IPC（命名管道 / loopback socket）
               │ 协议：{"op":"click","hwnd":...,"risk":"L2","token":...}
┌──────────────▼─────────────────────────────────────────────┐
│  视觉/操作外挂（独立进程，可被杀、可重启）                       │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  capture_worker.exe（C++，独立子进程）                   │  │
│  │  - Graphics Capture 单窗口捕获（被遮挡也可见）            │  │
│  │  - 输出「当前帧 + 窗口几何/DPI 信息」                      │  │
│  │  - RAII（ComPtr / unique_ptr），崩溃可被杀重启            │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Python 主逻辑                                          │  │
│  │  - 视觉 provider 适配层（openai/anthropic/llama_cpp）     │  │
│  │  - 坐标闭环换算（反缩放 + DPI + ClientToScreen）           │  │
│  │  - 动作-验证-收敛 三元组                                  │  │
│  │  - SendInput 键鼠注入 / UIA（可选加速）                    │  │
│  └───────────────────────────────────────────────────────┘  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  安全裁决器（独立，Agent 够不着）                          │  │
│  │  - 分级确认（L0~L3）                                     │  │
│  │  - 三态熔断机（CLOSED/OPEN/HALF-OPEN）                    │  │
│  │  - 全局热键物理熔断（最高优先级旁路）                       │  │
│  │  - 前台在场检测（GetLastInputInfo）                       │  │
│  │  - override 接管 / delegate 预授权执行                     │  │
│  └───────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────┘
```

**关键设计意图：**

- **隔离**：外挂进程崩溃 / 被入侵 / 失控，杀掉即可，主架构无感。外挂没有自授权能力。
- **最小权限**：普通用户权限，无管理员、无驱动、无全局钩子——「不需要提权」本身就是最强的安全属性，能造成的破坏有物理上限。
- **绿灯**：主架构发「一次性令牌 + 风险等级 + 有效期」的指令，外挂只执行令牌合法的指令。
- **三层熔断**：主架构可「掐断 IPC + 吊销令牌 + 杀掉外挂进程」，三层都能毫秒级熔断。

---

## 4. IPC 协议（自定规范：XML 信封 + JSON 负载）

### 4.0 设计选型（已定案）

消息格式**自定义规范**：外层用 **XML 标签作信封**（承载控制元信息），内层用 **JSON 承载业务数据**（`<![CDATA[ ... ]]>` 包裹）。理由：

- **人类可读 + 机器可解析**：XML 标签让审计日志可以直接用肉眼看、用 grep 过滤；JSON 让程序解析复杂嵌套数据零成本。
- **信封/负载分离**：XML 属性只放「控制字段」（op / risk / token / status / ts / version），业务数据（坐标、文本、窗口信息）全部沉到 JSON 里，互不污染。
- **自描述 + 可演进**：`version` 属性允许协议升级，旧版本外挂能拒绝不认识的新版本消息，而不是默默误解。
- **都用成熟解析器**：XML 用 `xml.etree`/`lxml`，JSON 用 `json`，不手写协议，规避解析 bug。

> 注意：这是**消息格式**层。传输层（命名管道 / loopback socket / 共享内存）属于实现细节，见第 12 节待定项。

### 4.1 指令消息（主架构 → 外挂）

```xml
<vision-op version="1.0" op="click" risk="L2" token="<一次性令牌，用后即焚>" ttl_ms="3000" ts="1750000000">
  <payload><![CDATA[
    {"hwnd": 123456, "x": 320, "y": 180, "text": ""}
  ]]></payload>
</vision-op>
```

- 根元素 `<vision-op>`：标识这是一条「主架构 → 外挂」的操作请求。
- XML 属性 = 控制字段：`op`（操作类型）、`risk`（L0~L3）、`token`（一次性授权令牌）、`ttl_ms`（有效期）、`version`（协议版本）、`ts`（时间戳）。
- `<payload>` 内 JSON = 业务数据：坐标、文本、窗口句柄等，可任意嵌套。

### 4.2 结果 / 审计消息（外挂 → 主架构）

```xml
<vision-result version="1.0" op="click" status="approved" risk="L2" ts="1750000000">
  <payload><![CDATA[
    {"hwnd": 123456, "user_approved": true, "attempt": 1, "result": "预期弹窗已出现"}
  ]]></payload>
</vision-result>
```

- 根元素 `<vision-result>`：标识这是一条「外挂 → 主架构」的结果/审计回执。
- `status` 枚举：`approved | rejected | vetoed | circuit_open | timeout`。

**审计铁律**：每一次 L1+ 操作（含被否决、被熔断、用户打断的）都必须落一条不可被 Agent 篡改的审计记录。审计日志是「一等公民」，和熔断器同等重要。

---

## 5. 操作分级 L0~L3 与「动作-验证-收敛」三元组

### 5.1 分级（风险越高，确认门槛越高）

| 级别 | 定义 | 确认方式 |
|---|---|---|
| **L0** | 只读（读控件文本/值、截图、视觉描述） | 不确认，直接执行，可随时打断 |
| **L1** | 无副作用点击（切换 tab、展开菜单、移动鼠标） | 静默确认（短 TTL），用户可打断 |
| **L2** | 有副作用写入（填表单、点「删除/发送」） | **必须显式确认**（用户点「批准」才动） |
| **L3** | 破坏性/不可逆（清空、提交、关闭应用、写注册表） | 双因子确认 + 熔断器独立闸门，**永不免确认** |

### 5.2 「动作-验证-收敛」三元组（唯一执行范式）

**不允许存在「只动作不验证」的路径。** 每次操作严格遵循：

```
① 动作：算坐标 → 执行（L1/L2 动作，先过裁决器）
② 验证：重新捕获窗口 → 视觉/状态比对（L0 只读，零风险）
③ 收敛：
    预期状态变化发生 → 记录成功，继续
    未发生 / 不确定 → 有限重试（见下），超过上限则停手上报
```

### 5.3 收敛策略（已定案：允许 3 次失败后报告）

```
失败计数 attempt = 1..3
    attempt ≤ 3 → 自动微调坐标 / 降级策略，重试
    attempt > 3 → 立即停手，把「我点了什么、看到了什么、为什么失败」
                  完整上报给用户，等待用户命令（不再自动执行）
```

**为什么「验证」同时是安全机制**：验证永远是 L0 只读，零副作用。所以「动作-验证-收敛」这条环，既让坐标能收敛（解决 DPI 偏移），又让每次操作天然带一个安全检查点——**负反馈和熔断不是两套东西，是同一个环**。

---

## 6. 熔断三态机 + 物理熔断 + 前台在场检测

### 6.1 三态熔断机（circuit breaker）

```
CLOSED（正常放行）
   ↓ 连续 N 次被用户否决 / 出现异常
OPEN（熔断：拒绝一切 L1+ 操作，只保留 L0 读）
   ↓ 冷却时间 T + 用户手动复位
HALF-OPEN（试探：允许一个 L1 操作，成功→CLOSED，失败→OPEN）
```

**铁律**：熔断后只允许「用户手动复位」或「冷却 + 试探」，**绝不允许 Agent 自己把自己从熔断里拉出来**——这就是「失效安全」。

### 6.2 全局热键物理熔断（最终兜底）

- 用户按 **`Ctrl+End`** → **立即熔断**。
  - 选 `Ctrl+End` 的原因：部分电脑（尤其笔记本）没有 `Scroll Lock` 键，`Ctrl+End` 在所有标准键盘上都有、且日常极少与其他软件冲突。
- 信号**不经过任何 IPC、不经过 Agent、不经过主架构**，直接让外挂进入 OPEN 态并杀掉当前操作。
- 热键监听必须在外挂进程内部、最高优先级、**不可被 Agent 干预**。
- 这是「失效安全」的物理级旁路：即使 Agent 卡死、IPC 卡死、主架构卡死，用户按一个键，外挂物理级停手。

### 6.3 前台在场检测（禁止后台运行，但做成用户可配置项）

- 外挂周期性（约 1s）检测 `GetLastInputInfo()`（键鼠空闲时间）。
- **默认空闲阈值 150 秒**：超过则自动进入暂停态，拒绝一切 L1+ 操作。
- **做成设置项，由用户自选**（两个维度独立可调）：
  ```python
  idle_timeout_sec   = 150    # 空闲多久判定为「用户离开」，默认 150
  idle_allow_operate = False  # 空闲后是否允许 Agent 继续操作电脑
                              #   False（默认）：用户离开即锁死，只保留 L0 读
                              #   True：允许继续操作（有风险，用户明知后主动开启）
  ```
- 把「不允许后台运行」从口号变成机制，但同时尊重用户自己的风险偏好：**默认「离开即锁死」，用户若确有「人离开但想让它继续跑」的需求，可主动把 `idle_allow_operate` 打开**——这是用户第一权威的体现，风险由用户知情自担。

### 6.4 用户介入检测（用户一动，Agent 立即挂起 → override）

- 外挂执行操作期间，一旦检测到**用户主动的键鼠输入**，立即判定「用户要接管」，进入 **override（用户接管）** 状态：
  - **智能体挂起**：暂停当前操作序列，不再执行后续动作（当前这一帧原子动作完成后停手）。
  - 由用户操作电脑，直到用户明确指示恢复。
  - 挂起本身就是「用户第一权威」的运行时体现——用户永远优先。
- 与 6.3 在场检测的分工：
  - **6.3 在场检测**（`GetLastInputInfo` 空闲超时）→ 管「用户离开没有」→ 控制**能不能后台跑**。
  - **6.4 介入检测**（用户键鼠输入事件）→ 管「用户正在操作没有」→ 控制**谁在操作**。
  - 两者殊途同归：**只要用户在场且有操作意图，Agent 就让路。**

---

## 7. 控制权与确认权协议（override 用户接管 / delegate 预授权）

> 术语定案（本轮评审）：**override = 用户接管**（用户主动键鼠输入 → Agent 挂起，见 6.4）；**delegate = 用户让渡确认权**（预授权，免逐次确认）。二者是**两个正交维度**，不再混用「override」这一个词。

### 7.1 delegate 的语义（用户主动授予，非系统自动判定）

delegate 的语义是**「用户主动让渡确认权」**，而不是「系统自动判定要不要超驰用户」。因此不存在「阈值大了忽略用户、小了总被超驰」的两难——**用户永远是默认权威，delegate 是用户主动说「接下来这段时间 / 这个窗口的操作我批准了，别问我」**。

### 7.2 可调参数清单（每一项独立可调、独立观察）

```python
delegate_window_sec   = 300    # 用户主动授予的免确认时长
delegate_scope        = "window"  # 让渡范围：window | app | session
delegate_max_risk     = "L2"   # 让渡最多覆盖到哪一级（L3 永不免确认）
auto_tighten_threshold = 3     # 连续被否决 N 次 → 自动收紧回「事事确认」
cooldown_after_veto   = 60     # 被否决后冷却多久才允许再次申请
```

- 有效期到 → 自动回到「事事确认」；用户随时喊停 → 立即回到「事事确认」+ 触发一次熔断记录。
- 阈值问题被消解：**不是系统在猜要不要超驰用户，而是用户自己决定何时、在多大范围、让渡多久**。系统只是执行「delegate 协议」，并保证「到期 / 喊停必复位」。

### 7.3 标定台（最小化调试程序）

delegate / override 接管 / 熔断 / 坐标换算**全部脱离 Agent 链路，在独立「标定台」里 dry-run 调试**：

```
标定台（独立小工具，不碰 Agent、不碰真实窗口）
  输入：一个「操作脚本」（模拟 Agent 会发出的操作序列 + 风险等级 + delegate 参数）
  输出：
    - 每一步的裁决结果（放行/拒绝/熔断）
    - 误放行 / 误拦截 统计
    - 坐标换算结果（喂假坐标，看换算对不对，不真点）
  用途：
    - 单独调 delegate_* 参数
    - 单独测熔断状态机转移（CLOSED→OPEN→HALF-OPEN）
    - 单独测坐标换算
```

核心价值：**「delegate 逻辑」和「真实操作」在调试阶段彻底分离**，调参不经过视觉/捕获/输入重模块，全程 dry-run、零真实副作用、可复现。标定台后续可直接升级为「影子模式」引擎（标定台=假 Agent，影子模式=真 Agent + 假执行，裁决逻辑同一套）。

---

## 8. 坐标闭环（反缩放 + DPI + ClientToScreen）

### 8.1 完整换算链

```
视觉模型在「截图」上给坐标 (x_img, y_img)
        │  ① 反缩放：x_phys = x_img * (frame_width / image_sent_width)
        ▼
物理像素坐标
        │  ② DPI 换算：x_dip = x_phys * 96 / dpi
        ▼
逻辑坐标（96 DPI 基准）
        │  ③ ClientToScreen(hwnd, &pt)
        ▼
屏幕坐标  ← SendInput 要的是这个
```

### 8.2 三个必须拿准的权威数据（不猜）

1. **截图真实尺寸**：`Direct3D11CaptureFrame.ContentSize`（物理像素宽高）。若喂给视觉模型前做了缩放，必须精确记录反缩放比例，**绝不能省略**，否则 100% 点偏。
2. **窗口 DPI**：`GetDpiForWindow(hwnd)`。物理像素 → 逻辑坐标。
3. **客户区 → 屏幕**：`ClientToScreen(hwnd, &pt)` 自动把窗口位置、标题栏、边框算进去。

### 8.3 三个隐蔽的坑（提前预警）

1. **进程 DPI Awareness**：外挂进程启动第一行必须 `SetProcessDpiAwarenessContext(PER_MONITOR_AWARE_V2)`，否则混插不同 DPI 显示器（100% + 150%）时拿到的 DPI 是虚拟化错值，必点偏。
2. **窗口拖动/缩放/跨屏**：窗口位置/大小/DPI 变化后，缓存坐标立即失效，必须监听 `WM_MOVE`/`WM_SIZE`/DPI 变更事件，触发坐标链重算。
3. **多显示器负坐标**：副屏在主屏左边时屏幕坐标是负的。

### 8.4 与负反馈的关系

坐标换算**不需要做到像素级完美**，只需做到「负反馈能收敛」的程度——DPI 反缩放公式哪怕初始有 5~10 像素偏差，只要验证环节能发现「点偏了」，系统就自动微调再试，或降级为「请用户指一下正确位置」。DPI 偏移从「必须做对」降级为「做个够接近的初值 + 可靠的验证环」。

---

## 9. 视觉 provider 适配层

### 9.1 统一接口签名

```python
# 所有 provider 实现这一个签名，主逻辑只认它
def describe(data: bytes, ext: str, mime: str, prompt: str, cfg: dict) -> str
# data=图片二进制  ext=扩展名  mime=MIME  prompt=给模型的指令  cfg=该 provider 配置
# 返回：模型输出的文字描述（或结构化 JSON，如「坐标 + 语义」）
```

### 9.2 三类 adapter（第一版全做）

| adapter | 覆盖范围 | 说明 |
|---|---|---|
| `openai_compatible` | OpenAI / Qwen-VL / GLM-4V / Ollama(兼容模式) / vLLM | 走 `/chat/completions`，`content` 里放 `text` + `image_url`(data URL) |
| `anthropic` | Claude | `content` 里放 `{"type":"image","source":{"type":"base64",...}}` |
| `llama_cpp` | llama.cpp 本地 raw 接口 | `/completion` 或 OpenAI 兼容模式二选一 |

### 9.3 优先级

```
内置 provider（openai_compatible / anthropic / llama_cpp，含 key/base_url）
  → 本地回调（register_vision_handler，保留现有能力）
  → 外部 URL（现有 vision_service_url，保留 fallback）
```

- **把「格式转换」和「调用」拆开**：每个 adapter 核心就是 `to_messages()` + `parse()`，新增 provider = 新增一个 adapter，主逻辑零改动。
- 隐私提示：图片二进制会 base64 后发往所配置的视觉服务，云端 provider 时需在设置面板明确标注「图片将发送至视觉服务方」；本地 llama.cpp 提供完全离线选项。

### 9.4 主动看图（打通 read_file）

- `tools.py` 层：在 `read_file` 工具描述里声明「也支持读取图片，返回视觉描述」，引导模型主动读图。
- `async_executor.py` 层：`read_file` 遇到 `is_visual_ext(ext)` 时转调视觉层，**异步 + 独立超时 + 按 `路径 + mtime` 缓存**（Agent 的 ReAct 循环会反复读同一张图，必须缓存）。

---

## 10. 安全裁决器（从零设计，不复用现有 LLM 安全框架）

### 10.1 与现有安全系统的关系

**明确声明**：本模块的安全裁决器**不复用** `permission_cascade` / `norp_safe` / `jailbreak_guard` / `runtime_check` 的任何代码、类、状态机、阈值。

**唯一允许的「对齐」**：命名风格与日志约定（方便用户与维护者认知一致）。

**冲突规避**（用户要求「两套安全系统别打架」）：
- 现有 LLM 安全系统管「内容/代码级」：prompt 注入、越狱、危险命令、路径越界。
- 新安全系统管「物理副作用级」：键鼠输入、窗口操作、屏幕信息外泄。
- 两者**职责不重叠**：现有系统在「Agent 产生操作意图」之前拦截，新系统在「操作意图落地为物理动作」之前拦截。时序上各管一段，不冲突。
- 落地为一条规则：**外挂只处理「已经过 LLM 安全系统放行的合法工具调用」**，但外挂**不信任**这一放行，仍然独立走自己的分级 + 熔断裁决。

### 10.2 裁决权归属

**裁决权 = 「这个操作能不能执行」的最终判断权放在谁手里。**

- 真正决定「放不放行」的裁决逻辑，跑在**外挂进程**里（独立裁决器）。
- Agent 只能「申请」，不能「裁决自己」——它没有「自己批准自己」的能力。

**为什么必须如此（通俗解释）**：如果裁决权在 Agent 手里，那 Agent 被 prompt 注入、判断失误、被诱导时，会「自己给自己开绿灯」，前面定的分级、熔断、热键全部形同虚设（因为它自己就是那个说了算的人）。把裁决权剥到独立进程里，安全才真正「凌驾」于 Agent 之上，而不是「寄希望于 Agent 自觉」。

**类比**：银行金库的钥匙不在保安（Agent）手里，而在独立系统（裁决器）手里。保安可以请求开门，但门只认独立系统发的授权。即使保安被收买（Agent 被攻破），他最多只能「不断敲门」，门不开就什么都拿不走。

### 10.3 审计

每次 L1+ 操作落一条不可被 Agent 篡改的审计记录：时间、目标窗口、操作类型、风险级、用户是否批准、尝试次数、当时视觉输入摘要、最终结果。用户事后可**追溯「Agent 到底做了什么、为什么做」**，与事前阻止同等重要。

---

## 11. 实现里程碑

### 阶段一（能真正用起来，工作量最小）

1. `vision.py` 增加内置 provider 适配层（三个 adapter + 统一 `describe()` 签名）。
2. `config.py` + 设置面板加 `vision_provider / vision_model / vision_api_key / vision_base_url`。
3. `tools.py` + `async_executor.py` 打通 `read_file` → 视觉层（异步 + 超时 + 缓存）。
4. C++ `capture_worker.exe`（Graphics Capture 单窗口捕获，RAII）。
5. 标定台（override / 熔断 / 坐标换算 dry-run）。

### 阶段二（操作闭环）

6. Python 主逻辑：坐标闭环 + SendInput + 动作-验证-收敛。
7. 安全裁决器（分级 L0~L3 + 三态熔断 + 热键 + 在场检测 + override）。
8. IPC 协议 + 审计日志。
9. 插件接入（通过 plugin_system 注册视觉/操作工具）。

### 阶段三（可选增强）

10. 视频首帧方案。
11. 官方 vision 插件示例（解决 EXE 下本地回调不可用问题）。

---

## 12. 需要反复测试优化的细节（不阻塞设计）

- 熔断「连续 N 次否决」的 N 值（定为 3）。
- 冷却时间 T 值（定为 60s）。
- IPC 传输层具体通道（命名管道 vs loopback socket vs 共享内存）——消息格式已定为「XML 信封 + JSON 负载」，传输层待定。

### 已敲定的决策

| 项 | 决策 |
|---|---|
| 熔断热键 | `Ctrl+End`（兼容无 Scroll Lock 的笔记本键盘） |
| 空闲检测阈值 | 默认 150 秒，做成设置项 |
| 空闲后是否继续操作 | 默认「否」，用户可主动开启（`idle_allow_operate`） |
| IPC 消息格式 | 自定规范：XML 标签信封 + JSON 负载（CDATA 包裹） |
| 验证失败重试 | 允许 3 次失败后停手上报，等待用户命令 |
